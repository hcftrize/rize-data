type BondTimeMarkerSnapshot @entity(immutable: true) {
  id: ID!
  nftId: BigInt!
  timeMarker: BigInt!
  amount: BigDecimal!
  poolId: Int!
  amountAdded: BigDecimal!
  date: String!
  blockNumber: BigInt!
  timestamp: BigInt!
  txHash: Bytes!
}
